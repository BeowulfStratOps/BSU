mode: ContinuousDeployment
next-version: 5.1.0
tag-prefix: '[vV]?'

branches:
  master:
    regex: ^master$
    label: dev
    increment: Patch
    is-main-branch: true
    prevent-increment:
      of-merged-branch: true

ignore:
  sha: []
