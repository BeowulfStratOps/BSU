<?xml version="1.0" encoding="utf-8" ?>
<nlog xmlns="http://www.nlog-project.org/schemas/NLog.xsd"
      xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance">

    <targets>
        <target name="logfile" deleteOldFileOnStartup="true" xsi:type="File" fileName="log.log"
        layout="${time}|${level:uppercase=true}|${threadname}|${logger:shortName=true}|${event-properties:item=uid}|${message}"/>
    </targets>

    <rules>
        <logger name="*" minlevel="Trace" writeTo="logfile" />
    </rules>
</nlog>
