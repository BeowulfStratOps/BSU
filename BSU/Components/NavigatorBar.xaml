<UserControl x:Class="BSU.GUI.Components.NavigatorBar"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:BSU.GUI.Components"
             xmlns:gui="clr-namespace:BSU.GUI"
             xmlns:converters="clr-namespace:BSU.GUI.Converters"
             mc:Ignorable="d"
             x:Name="Uc"
             d:DesignHeight="25" d:DesignWidth="400" d:DataContext="{d:DesignInstance local:NavigatorBar}">
    <Grid DataContext="{Binding ElementName=Uc}">
        <Grid.Resources>
            <converters:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter"></converters:BoolToVisibilityConverter>
        </Grid.Resources>
        <DockPanel Background="{DynamicResource BackgroundColor1Brush}">
            <StackPanel DockPanel.Dock="Left" Orientation="Horizontal" Cursor="Hand" MouseLeftButtonDown="Back_Click" ToolTip="Back" Visibility="{Binding CanGoBack, Converter={StaticResource BoolToVisibilityConverter}}">
                <gui:FontIcon FontWeight="Black" FontSize="12" Icon="E96F" />
                <TextBlock Margin="0 3" FontWeight="SemiBold" FontSize="14" VerticalAlignment="Center">Back</TextBlock>
            </StackPanel>
            <StackPanel DockPanel.Dock="Right" Orientation="Horizontal" Cursor="Hand" MouseLeftButtonDown="Storages_Click" ToolTip="Show Download Locations" Visibility="{Binding CanGoToStorages, Converter={StaticResource BoolToVisibilityConverter}}">
                <TextBlock Margin="0 3" FontWeight="SemiBold" FontSize="14" VerticalAlignment="Center">Storage</TextBlock>
                <gui:FontIcon FontWeight="Black" FontSize="12" Icon="E970" />
            </StackPanel>
            <TextBlock FontWeight="Bold" FontSize="18" HorizontalAlignment="Center" Text="{Binding Title, FallbackValue=Page, TargetNullValue=???}" />
        </DockPanel>
    </Grid>
</UserControl>
